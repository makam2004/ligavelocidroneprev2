body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background: url('/background.jpg') no-repeat center center fixed;
  background-size: cover;
  color: #fff;
}

.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 20px;
  background-color: rgba(0, 0, 0, 0.5);
}

.top-bar .left, .top-bar .right {
  flex: 1;
  display: flex;
  justify-content: center;
}

.top-bar .center {
  flex: 2;
  text-align: center;
}

#titulo {
  font-size: 32px;
  font-weight: bold;
  margin: 0;
}

#popup, #popupAlta {
  display: none;
  position: fixed;
  top: 20%;
  left: 50%;
  transform: translateX(-50%);
  background-color: #222;
  padding: 20px;
  border-radius: 10px;
  z-index: 1000;
  width: 320px;
  max-width: 90%;
  color: white;
}

#popup pre {
  white-space: pre-wrap;
  font-size: 14px;
}

.overlay {
  display: none;
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  background: rgba(0, 0, 0, 0.7);
  z-index: 900;
}

.card {
  background-color: rgba(255,255,255,0.05);
  padding: 15px;
  border-radius: 10px;
  margin: 10px 0;
  width: 48%;
}

table {
  width: 100%;
  border-collapse: collapse;
  font-size: 14px;
}

th, td {
  padding: 6px;
  text-align: center;
}

.mejorado { background-color: #0a0; }
.empeorado { background-color: #a00; }

.tracks-wrapper, .rankings-wrapper {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 20px;
  margin: 20px;
}

button {
  background: #007bff;
  color: white;
  border: none;
  padding: 8px 12px;
  border-radius: 5px;
  cursor: pointer;
}
