.top-bar {
  width: 100%;
  display: flex;
  justify-content: space-between;
  padding: 15px 40px;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;
  background: rgba(0, 0, 0, 0.75);
}

.top-bar .left,
.top-bar .right {
  display: flex;
  align-items: center;
}

.container {
  margin-top: 100px;
}

body {
  font-family: 'Segoe UI', sans-serif;
  margin: 0;
  background: url('background.jpg') no-repeat center center fixed;
  background-size: cover;
  color: #ffffff;
  backdrop-filter: brightness(0.6);
}

.container {
  max-width: 1100px;
  margin: auto;
  padding: 20px;
  background-color: rgba(0,0,0,0.7);
  border-radius: 12px;
  margin-top: 30px;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

header img {
  height: 50px;
}

h1 {
  text-align: center;
  font-size: 28px;
  margin: 0;
  padding: 10px 0;
}

button {
  padding: 10px 16px;
  font-size: 14px;
  border: none;
  border-radius: 6px;
  background-color: #007acc;
  color: white;
  cursor: pointer;
}

button:hover {
  background-color: #005fa3;
}

table {
  width: 100%;
  margin-top: 10px;
  border-collapse: collapse;
  background-color: #222;
}

th, td {
  padding: 10px;
  border-bottom: 1px solid #444;
}

th {
  background-color: #333;
}

tr.mejorado td { color: #00ff00; }
tr.empeorado td { color: #ff4444; }

.card {
  margin-top: 30px;
  background-color: #111;
  padding: 20px;
  border-radius: 10px;
}

#popup, #popupAlta {
  position: fixed;
  top: 20%;
  left: 50%;
  transform: translate(-50%, -20%);
  background: #222;
  padding: 20px;
  border-radius: 10px;
  display: none;
  z-index: 999;
  max-width: 800px;
  max-height: 80vh;
  overflow-y: auto;
  box-shadow: 0 0 20px rgba(0,0,0,0.6);
}

.overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  z-index: 998;
  display: none;
}
